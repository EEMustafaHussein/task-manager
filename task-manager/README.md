# Task Manager API

A robust Task Manager backend API built with FastAPI and SQLModel.

## Features
- User Authentication (JWT) & Role-Based Access Control (Admin / User)
- Full CRUD operations for Tasks with priorities and status tracking.

## How to Run
1. Install dependencies:
   ```bash
   pip install -r requirements.txt